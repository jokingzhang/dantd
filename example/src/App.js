import React, { Component } from "react";

import { EmptyLine, Button } from "dantd";

export default class App extends Component {
  render() {
    return (
      <div>
        <Button>好呢</Button>
        <EmptyLine />
        <Button>好呢</Button>
      </div>
    );
  }
}
